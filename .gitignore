.DS_Store
node_modules/
.env
.vercel
.env*

# One-time local data scripts + artifacts (run locally, never deployed)
generate-deck.js
push-deck.js
migrate-taxonomy.js
generated-deck.json
gen.log
audit-taxonomy.js
audit-taxonomy-report.json
audio-backfill/
# Legacy server.js's local file-based flashcard storage (FLASHCARDS_PATH) —
# populated by running the app against `npm start` locally, never real data.
data/flashcards.json
data/flashcards.json.bak
